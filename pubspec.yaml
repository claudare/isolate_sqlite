name: isolate_sqlite
description: Run an SQLite database in the isolate the easy way.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.3

# Add regular dependencies here.
dependencies:
  meta: ^1.18.2
  sqlite3: ^3.2.0
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
